<!-- .lenght coz succes is always emtpty array -->
<% if(success && success.length) { %>
    <div class="flash succes" role="alert">
        <%= success %>


    </div>
    <% } %>

        <% if(error && error.length) { %>
            <div class="flash error" role="alert">
                <%= error %>


            </div>
            <% } %>